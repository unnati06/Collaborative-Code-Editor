name: Deploy

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Simulated Deployment
        run: |
          echo "Application validated"
          echo "Docker image built successfully"
          echo "Ready for production deployment"gi